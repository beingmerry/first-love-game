function love.conf(t)
    t.window.title = "Super LÖVE Adventure" -- The title of the window the game is in
    t.window.width = 800                    -- The window width
    t.window.height = 600                   -- The window height
end
